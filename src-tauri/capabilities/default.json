{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The desktop window only needs its Rust command interface.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
