{
  "name": "agent-skill-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Codex-first visual workspace for authoring, auditing, comparing, and migrating Agent Skills.",
  "scripts": {
    "dev": "tauri dev",
    "test": "node --test test/*.test.mjs && npm run web:build && cargo test --manifest-path src-tauri/Cargo.toml",
    "web:dev": "vite",
    "web:build": "vite build",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "lucide": "^0.468.0",
    "mdast-util-from-markdown": "^2.0.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "vite": "^8.1.5"
  }
}
